query getSight ($id: String!) {
    getSight (id: $id) {
        id,
        creationDate,
        title,
        type,
        subtext,
        location,
        long,
        lat,
        images,
    }

}