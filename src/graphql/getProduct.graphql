query getProduct ($id: String!) {
    getProduct (id: $id) {
        id,
        title,
        creationDate,
        paymentPricePlan,       
        price,
        minmonth,
        active    
    }
}